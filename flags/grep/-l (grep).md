## Display Matching Filenames Using Grep

We can display files that contain the given string/pattern:

```bash
grep -l "python" *
```

or:

```bash
grep -l "unix" f1.txt f2.txt f3.txt f4.txt
```

Output:

![[grep -l example.png]]

#grep #flags 