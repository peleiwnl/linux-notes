## Display Matched Pattern using grep

By default, grep displays the entire line which has the matched string. We can make the grep display only the matched string using the -o option.

```bash
grep -o "Python" grepexample1.txt
```

Output:

![[grep -o example.png]]

#grep #flags 