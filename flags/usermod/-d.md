Changes a user's home directory when combined with usermod. You can specify the new path for the home directory.

## Syntax:

```
usermod -d new_home_directory_path username
```

### Command

```
usermod -d new_home_directory test
```

#usermod #flags 