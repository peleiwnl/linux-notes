Removes multiple consecutive empty lines from the output. Helps to produce a cleaner and more readable file display

## Syntax:

```
cat -s file_name
```

## Example

```
cat -s text3.txt
```

![[cat -s example.png]]