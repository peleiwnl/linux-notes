## Inverting the pattern match using grep

We can display lines that are not matched with the specified search string pattern using the `-v` option.

```bash
grep -v "Python" grepexample1.txt
```

Output:
