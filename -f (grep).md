Used to search for multiple patterns, listed in a separate file, within another target file.

Command:
```bash
grep -f pattern.txt file.txt
```

- `-f pattern.txt` tells `grep` to read patterns from the file `file1`

#flags #grep 