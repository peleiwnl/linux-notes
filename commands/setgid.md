## SET Group ID

Allows files to run under fule's group permissions (or ensures the files are created in a directory inherits the group of the directory). Here's the command for the same:

```
chmod g+s directoryname
```



#commands #permissions 