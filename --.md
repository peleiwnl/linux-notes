When a filename begins with a dash (-), Linux may treat it as a command option. The -- option is used with cat to tell the system that what follows is a filename, not an option.

## Syntax:

```
cat -- file_name
```

## Example:

```
cat -- -text4
```

![[cat -- flag example.png]]

#flags #cat 