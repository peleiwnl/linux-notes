Cron can be used for job scheduling. 

## Format

A cron expression is a simple string comprised of between 6 and 7 fields, each separated by a white space:

```
<seconds> <minutes> <hours> <day-of-month> <month> <day-of-week> <year>
```

### Characters used in Cron Expressions

![[special characters in cron expressions.png]]

With the above fields, a combination of values can be used to create the desired schedules using Cron expressions. 

